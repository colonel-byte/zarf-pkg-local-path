---
# yaml-language-server: $schema=https://raw.githubusercontent.com/zarf-dev/zarf/main/zarf.schema.json
kind: ZarfPackageConfig
metadata:
  name: zarf-package-local-path-provider-base
components:
  - name: local-path-provider-chart
    charts:
      - name: local-path-provider
        namespace: local-path-storage
        releaseName: local-path-provider
        url: oci://ghcr.io/rancher/local-path-provisioner/charts/local-path-provisioner
        # renovate: datasource=docker depName=ghcr.io/rancher/local-path-provisioner/charts/local-path-provisioner
        version: 0.0.31
        valuesFiles:
          - ../values/base.yml
