# Zarf Package `local-path-provider`

Package for deploying local-path-provider with zarf.

## Note

We have changed the default path from `/opt/local-path-provisioner` to `/var/lib/local-path-provisioner`
